# ── Web framework ──────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17

# ── Database ───────────────────────────────────────────────
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10
neo4j==5.23.1

# ── Auth ───────────────────────────────────────────────────
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0

# ── Validation ─────────────────────────────────────────────
pydantic[email]==2.10.3
email-validator==2.2.0

# ── Async / Background tasks (Phase 2+) ────────────────────
celery==5.4.0
redis==5.2.1
flower==2.0.1

# ── Document processing (Phase 3) ──────────────────────────
pymupdf==1.24.14
pdfplumber==0.11.4
pytesseract==0.3.13
Pillow==11.0.0
python-docx==1.1.2
python-pptx==1.0.2
docxtpl==0.19.0
# unstructured: install separately when needed (version depends on Python)
# pip install "unstructured>=0.17.0"

# ── AI / LLM (Phase 4–5) ───────────────────────────────────
langchain==0.3.11
langchain-community==0.3.11
langgraph==0.2.56
langchain-groq==0.2.1
openai==1.57.4
anthropic==0.40.0
langfuse==2.56.2

# ── Vector DB / RAG (Phase 4) ──────────────────────────────
qdrant-client==1.12.1
sentence-transformers==3.3.1
FlagEmbedding==1.3.3

# ── Storage ────────────────────────────────────────────────
minio==7.2.11

# ── Observability ─────────────────────────────────────────
# Unpinned — let pip resolve compatible versions
opentelemetry-api
opentelemetry-sdk
opentelemetry-instrumentation-fastapi

# ── Utilities ─────────────────────────────────────────────
python-dotenv==1.0.1
httpx==0.28.1
aiofiles==24.1.0